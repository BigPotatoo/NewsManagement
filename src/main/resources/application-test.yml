logging:
  level:
    org.springframework: INFO

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/TestNewsDB
    username: postgres
    password: 123
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

